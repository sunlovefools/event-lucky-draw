// Kept separate from admin service modules because the confirmation dialog runs
// in the browser, while admin services depend on Node-only authentication code.
export const DELETE_ALL_DELEGATES_CONFIRMATION = "DELETE ALL DELEGATES";
